#include "testlib.h"
#include <bits/stdc++.h>

using namespace std;

#define forn(i, n) for (int i = 0; i < int(n); i++)


int main(int argc, char* argv[]) {
    registerValidation(argc, argv);

    int n = inf.readInt(1, (int)1e9, "n");
    inf.readSpace();
    int w = inf.readInt(1, (int)1e9, "w");
    inf.readSpace();
    int h = inf.readInt(1, (int)1e9, "h");
    inf.readEoln();

    inf.readEof();
}
