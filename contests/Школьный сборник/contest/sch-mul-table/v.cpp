#include "testlib.h"
#include <bits/stdc++.h>

using namespace std;

#define forn(i, n) for (int i = 0; i < int(n); i++)


int main(int argc, char* argv[]) {
    registerValidation(argc, argv);

    inf.readInt(1, (int)1e6, "a");
    inf.readSpace();
    inf.readInt(1, (int)1e6, "b");
    inf.readEoln();

    inf.readEof();
}
